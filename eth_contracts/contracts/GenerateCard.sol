pragma solidity ^0.4.17;

// Generation 0 Card Creation
contract Generate {

    // compiled attributes value for the card
    uint256 attributes;

    // number of cards to generate
    uint64 numberOfCards = 800

    function initialGeneration(numberOfCards) public returns (uint256) {
        uint256 cardID;

        for (cardID = 1; cardID <= attributes; cardID++) {
            
    }

}

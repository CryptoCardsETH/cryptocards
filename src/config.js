//don't put any actual values in this file!
//This just exposes env vars (which you can override via .env.local for testing) for easier development
export const API_BASE_URL = process.env.REACT_APP_API_BASE_URL;
